---
sitemap: false
---

<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
<title type="text">{{ site.title }}</title>
<generator uri="https://github.com/jekyll/jekyll">Jekyll</generator>
<link rel="self" type="application/atom+xml" href="{{ site.url }}/feed.xml" />
<link rel="alternate" type="text/html" href="{{ site.url }}" />
<updated>{{ site.time | date_to_xmlschema }}</updated>
<id>{{ site.url }}/</id>
<author>
  <name>{{ site.owner.name }}</name>
  <uri>{{ site.url }}/</uri>
  {% if site.owner.email %}<email>{{ site.owner.email }}</email>{% endif %}
</author>
{% assign combined = site.posts | concat: site.content %}
{% for post in combined limit:20 %}
{% if post.author %}
  {% assign author = site.data.authors[post.author] %}
{% else %}
  {% assign author = site.owner %}
{% endif %}
<entry>
  <title type="html"><![CDATA[{{ post.title | cdata_escape }}]]></title>
  <link rel="alternate" type="text/html" href="{{ site.url }}{{ post.url }}" />
  <id>{{ site.url }}{{ post.id | default: post.url }}</id>
  {% assign published_date = post.date | default: site.time %}
  {% assign updated_date = post.modified | default: post.date | default: site.time %}
  <published>{{ published_date | date_to_xmlschema }}</published>
  <updated>{{ updated_date | date_to_xmlschema }}</updated>
  <author>
    <name>{{ author.name }}</name>
    <uri>{{ site.url }}</uri>
    {% if author.email %}<email>{{ author.email }}</email>{% endif %}
  </author>
  <content type="html">
    {{ post.content | xml_escape }}
    {% include feed-footer.html %}
  </content>
</entry>
{% endfor %}
</feed>
